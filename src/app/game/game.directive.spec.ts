import { GameDirective } from './game.directive';

describe('GameDirective', () => {
  it('should create an instance', () => {
    const directive = new GameDirective();
    expect(directive).toBeTruthy();
  });
});
