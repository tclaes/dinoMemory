import { GameWonDirective } from './game-won.directive';

describe('GameWonDirective', () => {
  it('should create an instance', () => {
    const directive = new GameWonDirective();
    expect(directive).toBeTruthy();
  });
});
