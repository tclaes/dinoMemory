import { StandardDeckDirective } from './standard-deck.directive';

describe('StandardDeckDirective', () => {
  it('should create an instance', () => {
    const directive = new StandardDeckDirective();
    expect(directive).toBeTruthy();
  });
});
